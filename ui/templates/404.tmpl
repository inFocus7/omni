{{define "title"}}404 — OMNI{{end}}

{{define "body"}}
    <header class="site-header">
        <span></span>
        <h1><span class="title-text">OMNI</span></h1>
        <div style="display: flex; gap: 0.375rem;">
            <a href="/plugins" class="back-link">plugins</a>
            <a href="/settings" class="back-link">settings</a>
        </div>
    </header>
    <main class="plugins">
        <div class="empty-dashboard">
            <p class="empty-dashboard-title" style="font-family: var(--font-mono); font-size: 3rem; letter-spacing: -0.02em;">404</p>
            <p class="empty-dashboard-desc">The page you're looking for doesn't exist.</p>
            <a href="/" class="empty-dashboard-btn" style="text-decoration: none;">Go home</a>
        </div>
    </main>
{{end}}

{{define "scripts"}}{{end}}
