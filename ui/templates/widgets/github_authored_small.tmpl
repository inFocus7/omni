{{define "widget_github_authored_small"}}
<div class="widget-fill">
    <span class="stat-num">{{.AuthoredCount}}</span>
    <span class="stat-label">authored &middot; {{.Filter}}</span>
</div>
{{end}}
